server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/zakazi_pr9
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true 

springdoc:
  api-docs:
    enabled: true
  group-configs:
    - group: 'order-service' 
      paths-to-match: '/api/orders/**' 

